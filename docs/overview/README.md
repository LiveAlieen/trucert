# 项目概述

## 项目简介

TruCert是一个用于管理加密密钥和数字证书的综合工具，旨在简化证书的创建、存储、管理和验证过程。该系统支持多种加密算法，提供完整的证书生命周期管理功能，适用于需要安全通信和数据完整性验证的场景。

## 核心功能

- **密钥管理**：支持RSA和ECC密钥对的生成、存储、加载和管理
- **证书管理**：支持自签名证书和二级证书的生成、存储、加载和管理
- **文件签名**：支持使用私钥对文件进行签名
- **文件验证**：支持使用公钥或证书验证文件签名的有效性
- **配置管理**：提供灵活的配置存储和管理功能

## 系统架构

系统采用模块化设计，分为以下几个主要层次：

1. **业务逻辑层** (`src/trucert/core/business/`)：包含核心业务逻辑
   - `cert_manager.py`：证书管理核心功能
   - `key_manager.py`：密钥管理核心功能
   - `file_signer.py`：文件签名功能
   - `verifier.py`：验证功能
   - `config.py`：配置管理功能

2. **服务层** (`src/trucert/core/services/`)：作为业务逻辑层和外部调用之间的桥梁
   - `cert_service.py`：证书服务
   - `key_service.py`：密钥服务
   - `file_signer_service.py`：文件签名服务
   - `verifier_service.py`：验证服务
   - `config_service.py`：配置服务

3. **存储层** (`src/trucert/core/storage/`)：负责数据持久化
   - `storage_manager.py`：存储管理核心功能
   - `key_storage.py`：密钥存储功能
   - `cert_storage.py`：证书存储功能
   - `config_storage.py`：配置存储功能

4. **工具层** (`src/trucert/core/utils/`)：提供通用工具函数

## 技术栈

- **编程语言**：Python 3.8+
- **加密库**：cryptography
- **测试框架**：pytest
- **文档格式**：Markdown

## 目录结构

```
src/
└── trucert/
    ├── core/
    │   ├── business/       # 核心业务逻辑
    │   ├── services/       # 服务层
    │   ├── storage/        # 存储层
    │   └── utils/          # 工具函数
    ├── configs/            # 配置和存储目录
    │   ├── key/            # 密钥存储
    │   └── trust/          # 证书存储
    └── root_key/           # 根密钥存储

docs/                      # 文档目录
tests/                     # 测试目录
```

## 系统特点

1. **安全性**：使用行业标准的加密算法和安全存储实践
2. **灵活性**：支持多种加密算法和证书类型
3. **可扩展性**：模块化设计，易于添加新功能
4. **可靠性**：完善的错误处理和日志记录
5. **易用性**：提供简洁的API接口

## 应用场景

- **安全通信**：为TLS/SSL等安全通信提供证书支持
- **代码签名**：验证软件代码的完整性和真实性
- **文档签名**：确保文档的完整性和不可篡改性
- **身份验证**：用于系统间的身份验证

## 后续发展方向

- **GUI界面**：开发图形用户界面，提高用户体验
- **网络服务**：提供RESTful API，支持网络访问
- **证书链管理**：支持更复杂的证书链结构
- **硬件安全模块(HSM)集成**：支持使用HSM存储密钥
- **证书 revoke功能**：支持证书吊销列表(CRL)管理

## 项目状态

当前项目处于活跃开发阶段，核心功能已实现，正在不断完善和扩展中。